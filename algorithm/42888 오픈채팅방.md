# [42888 : 오픈채팅방](https://school.programmers.co.kr/learn/courses/30/lessons/42888)
https://school.programmers.co.kr/learn/courses/30/lessons/42888

## 아이디어
Simulation(Implementation), Parsing(String)

최종 출력되는 사람들의 닉네임은 전부 변경이 된 상태여야 한다 → 닉네임 변경을 전부 다 처리하고 출력을 해야한다.

`User` 클래스
```java
class User {

    String id, name;

    public User(String id, String name) {
        this.id = id;
        this.name = name;
    }
}
```

`User` 일급 컬렉션 클래스
```java
class Users {

    Map<String, User> users;

    public Users() {
        this.users = new HashMap<>();
    }

    /** 이름 변경도 같이 처리 */
    void addUserOrChangeName(String id, String name) {
        users.put(id, new User(id, name));
    }

    String getUserName(String id) {
        return users.get(id).name;
    }
}
```

`record` 처리 로직 구현
```java
class Solution {

    String[] solution(String[] record) {
        Users users = new Users();
        // 닉네임 변경 로직 먼저 처리
        Arrays.stream(record)
                .filter(s -> s.contains("Enter") || s.contains("Change"))
                .forEach(s -> {
                    StringTokenizer st = new StringTokenizer(s);
                    st.nextToken();
                    users.addUserOrChangeName(st.nextToken(), st.nextToken());
                });
        // 로그(출력)를 남기는 레코드를 후에 처리
        return Arrays.stream(record)
                .filter(s -> s.contains("Enter") || s.contains("Leave"))
                .map(s -> {
                    StringTokenizer st = new StringTokenizer(s);
                    String cmd = st.nextToken();
                    String id = st.nextToken();
                    return users.getUserName(id) + (cmd.equals("Enter") ? "님이 들어왔습니다." : "님이 나갔습니다.");
                })
                .toArray(String[]::new);
    }
}
```