# [68644 : 두 개 뽑아서 더하기](https://school.programmers.co.kr/learn/courses/30/lessons/68644)
https://school.programmers.co.kr/learn/courses/30/lessons/68644

## 아이디어
Brute Force, Implementation, Sorting

이중 `for` 문 완전 탐색으로 가능한 모든 합 경우의 수를 찾고 중복제거를 하며 `list`에 포함시킨다.
```python
def solution(numbers: list) -> list:
    answer = []
    for i in range(len(numbers)):
        for j in range(i + 1, len(numbers)):
            sum = numbers[i] + numbers[j]
            if sum not in answer:
                answer.append(sum)
    return sorted(answer)
```