# [84512 : 모음사전](https://school.programmers.co.kr/learn/courses/30/lessons/84512)
https://school.programmers.co.kr/learn/courses/30/lessons/84512

## 아이디어
Backtracking(Brute Force)

사전 단어들의 길이가 5로 제한이 되어 있으므로 모든 가능한 단어를 구해도 많지 않다.

재귀함수를 구현해 가능한 모든 단어들을 사전순으로 구해놓고 입력에 들어오는 단어가 몇 번째인지 찾는다.
```java
class Solution {

    static final String VOWELS = "AEIOU";
    static List<String> words = new ArrayList<>();

    void recur(char[] tree, int depth) {
        if (depth == 5) {
            return;
        }

        for (int i = 0; i < 5; i++) {
            tree[depth] = VOWELS.charAt(i);
            words.add(new String(tree, 0, depth + 1));
            recur(tree, depth + 1);
        }
    }

    int solution(String word) {
        recur(new char[5], 0);
        return words.indexOf(word) + 1;
    }
}
```